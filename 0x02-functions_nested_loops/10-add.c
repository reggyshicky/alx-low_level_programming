#include "main.h"
/**
 * add - Adds Integers
 * @a: Integer one
 * @b: Integer two
 * Return: returns the result of adding the two digits
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
